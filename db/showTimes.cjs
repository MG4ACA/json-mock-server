const showTimes = [
  // Showtimes will be generated dynamically or added via the admin panel
  // No hardcoded showtimes to avoid date conflicts and inconsistent data
];

module.exports = {
  showTimes,
};
